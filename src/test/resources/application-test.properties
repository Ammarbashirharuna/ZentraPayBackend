# Test profile for controller slice tests
jwt.secret=test-secret-key-for-controller-tests-minimum-256-bits-required-for-hmacsha
jwt.expiration=86400000
paystack.secret-key=
resend.api-key=test-key
resend.from-email=test@example.com
app.base-url=http://localhost:8080

# Minimize connection pool size to avoid exhausting PostgreSQL connections
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
